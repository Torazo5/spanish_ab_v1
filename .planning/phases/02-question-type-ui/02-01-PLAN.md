---
phase: 02-question-type-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/prompts/listening.ts
  - components/listening/McqRenderer.tsx
  - components/listening/TfngRenderer.tsx
  - components/listening/GapFillRenderer.tsx
autonomous: true
requirements: [LISTEN-01]

must_haves:
  truths:
    - "MCQ questions render as radio-style button groups with lettered options"
    - "True/False/Not Given questions render as 3-button toggle rows"
    - "Gap-fill questions render with inline input embedded in the sentence"
    - "GapFillQuestion type includes sentence field with ___ placeholder"
  artifacts:
    - path: "lib/types.ts"
      provides: "GapFillQuestion.sentence field"
      contains: "sentence: string"
    - path: "lib/prompts/listening.ts"
      provides: "Updated gap-fill schema with sentence field; tree-pine icon fix"
      contains: "sentence"
    - path: "components/listening/McqRenderer.tsx"
      provides: "MCQ radio-style option renderer"
      exports: ["McqRenderer"]
    - path: "components/listening/TfngRenderer.tsx"
      provides: "True/False/Not Given 3-button toggle"
      exports: ["TfngRenderer"]
    - path: "components/listening/GapFillRenderer.tsx"
      provides: "Inline blank input renderer"
      exports: ["GapFillRenderer"]
  key_links:
    - from: "components/listening/GapFillRenderer.tsx"
      to: "lib/types.ts"
      via: "imports GapFillQuestion with sentence field"
      pattern: "question\\.sentence\\.split"
---

<objective>
Fix GapFillQuestion schema gap, update generation prompt, and build three question type renderers (MCQ, True/False/Not Given, Gap-Fill).

Purpose: Establish the first three of five distinct question type components, and fix the schema prerequisite that blocks gap-fill rendering.
Output: Three renderer components ready for import by the dispatcher, plus corrected types and prompt.
</objective>

<execution_context>
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-question-type-ui/02-CONTEXT.md
@.planning/phases/02-question-type-ui/02-UI-SPEC.md
@.planning/phases/02-question-type-ui/02-RESEARCH.md

<interfaces>
<!-- Key types from lib/types.ts that renderers consume -->

From lib/types.ts:
```typescript
export interface McqQuestion {
  type: 'mcq'
  id: string
  text: string
  marks: number
  options: string[]
  correctIndex: number
}

export interface TrueFalseNotGivenQuestion {
  type: 'true-false-notgiven'
  id: string
  text: string
  marks: number
  correctAnswer: 'true' | 'false' | 'not-given'
}

export interface GapFillQuestion {
  type: 'gap-fill'
  id: string
  text: string
  marks: number
  acceptedAnswers: string[]
  // MISSING: sentence: string -- must be added in Task 1
}
```

From components/listening/QuestionPanel.tsx (existing pattern reference):
```typescript
// Row spacing: space-y-1.5 per question
// Label: text-sm text-zinc-300, number prefix: font-medium text-zinc-400
```

From app/listening/page.tsx (MarkSelector chip pattern -- visual reference):
```typescript
// Selected: 'border-sky-400/50 bg-gradient-to-br from-sky-400/20 via-sky-500/10 to-zinc-950 text-white shadow-[0_20px_45px_-30px_rgba(56,189,248,0.95)]'
// Unselected: 'border-white/10 bg-gradient-to-br from-zinc-800 via-zinc-900 to-zinc-950 text-zinc-200 hover:border-sky-400/25 hover:text-white'
// Disabled: 'pointer-events-none opacity-60'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GapFillQuestion schema and generation prompt</name>
  <files>lib/types.ts, lib/prompts/listening.ts</files>
  <read_first>
    - lib/types.ts (current GapFillQuestion interface at lines 88-94)
    - lib/prompts/listening.ts (current gap-fill JSON example and icon whitelist)
    - .planning/phases/02-question-type-ui/02-RESEARCH.md (Pitfall 2 and Pitfall 1 sections)
  </read_first>
  <action>
    1. In `lib/types.ts`, add `sentence: string` field to the `GapFillQuestion` interface, after the `text` field. The `sentence` field holds the full sentence with a `___` placeholder marking where the inline input goes (per D-05, D-06). Keep `text` for backward compatibility and question label display.

    2. In `lib/prompts/listening.ts`, update the gap-fill JSON example in `generateScriptPrompt`:
       - Change the example from using `text` for the sentence-with-blank to adding a `sentence` field:
         ```
         { "type": "gap-fill", "id": "q3", "text": "Fill in the missing word", "sentence": "Maria vive en una ciudad muy ___.", "marks": 1, "acceptedAnswers": ["grande", "bonita"] }
         ```
       - Update the description line below to: `- text: question prompt or instruction` and add `- sentence: the sentence from the script with ___ marking the blank`

    3. In the same `generateScriptPrompt` function, find the icon name whitelist line containing `tree` and replace `tree` with `tree-pine`. The exact line to change is:
       `- Use ONLY these Lucide icon names: bus, train, plane, car, bike, sun, cloud, cloud-rain, snowflake, utensils, apple, coffee, book, pencil, music, heart, home, building, tree, globe, users, phone, laptop, camera, clock, map-pin, shopping-bag, shirt, umbrella, thermometer`
       Change `tree` to `tree-pine` in that list.
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/types.ts contains `sentence: string` inside the GapFillQuestion interface
    - lib/prompts/listening.ts contains `"sentence": "` in the gap-fill JSON example
    - lib/prompts/listening.ts contains `tree-pine` and does NOT contain standalone `tree,` or `tree ` in the icon whitelist line (tree-pine is acceptable)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>GapFillQuestion has sentence field; prompt produces sentence with ___ placeholder; icon whitelist uses tree-pine instead of tree</done>
</task>

<task type="auto">
  <name>Task 2: Build McqRenderer and TfngRenderer components</name>
  <files>components/listening/McqRenderer.tsx, components/listening/TfngRenderer.tsx</files>
  <read_first>
    - lib/types.ts (McqQuestion and TrueFalseNotGivenQuestion interfaces)
    - app/listening/page.tsx (MarkSelector chip pattern for selected/unselected/disabled styles, lines 108-167)
    - .planning/phases/02-question-type-ui/02-UI-SPEC.md (McqQuestion renderer and TrueFalseNotGiven renderer sections)
  </read_first>
  <action>
    **McqRenderer.tsx** -- Create `components/listening/McqRenderer.tsx`:
    - `'use client'` directive at top
    - Import `McqQuestion` from `@/lib/types`
    - Props: `{ question: McqQuestion; answer: string; onAnswerChange: (id: string, value: string) => void; disabled?: boolean }`
    - Render question number and text as label: `<p className="text-sm text-zinc-300"><span className="font-medium text-zinc-400">{question.id}.</span> {question.text}</p>`
    - Render vertical list of option buttons with `space-y-2 mt-2` wrapper
    - Each option is a `<button>` with `w-full text-left` and the following classes:
      - Base: `rounded-xl border px-4 py-2.5 text-sm text-left w-full transition-all duration-200`
      - Unselected: `border-white/10 bg-gradient-to-br from-zinc-800 via-zinc-900 to-zinc-950 text-zinc-200 hover:border-sky-400/25 hover:text-white`
      - Selected: `border-sky-400/50 bg-gradient-to-br from-sky-400/20 via-sky-500/10 to-zinc-950 text-white shadow-[0_8px_20px_-10px_rgba(56,189,248,0.6)]`
      - Disabled: `pointer-events-none opacity-60` on the option list wrapper
    - Letter prefix per option: `<span className="text-[10px] font-semibold uppercase text-zinc-500 mr-2">{'ABCD'[i]}</span>` before option text
    - onClick: `onAnswerChange(question.id, i.toString())`
    - Selected when `answer === i.toString()`
    - Add `aria-pressed={answer === i.toString()}` to each option button
    - Export as named export: `export function McqRenderer`

    **TfngRenderer.tsx** -- Create `components/listening/TfngRenderer.tsx`:
    - `'use client'` directive at top
    - Import `TrueFalseNotGivenQuestion` from `@/lib/types`
    - Props: `{ question: TrueFalseNotGivenQuestion; answer: string; onAnswerChange: (id: string, value: string) => void; disabled?: boolean }`
    - Render question text same as McqRenderer
    - Render 3 toggle buttons in a row: `<div className="flex gap-2 mt-2" role="group" aria-label={question.text}>`
    - Button labels: `['True', 'False', 'Not Given']` mapping to values `['true', 'false', 'not-given']`
    - Button classes:
      - Base: `rounded-xl border px-4 py-2 text-xs font-semibold transition-all duration-200`
      - Unselected: `border-white/10 bg-gradient-to-br from-zinc-800 via-zinc-900 to-zinc-950 text-zinc-300 hover:border-sky-400/25 hover:text-white`
      - Selected: `border-sky-400/50 bg-gradient-to-br from-sky-400/20 via-sky-500/10 to-zinc-950 text-white shadow-[0_8px_20px_-10px_rgba(56,189,248,0.6)]`
    - Disabled: `pointer-events-none opacity-60` on the button row div
    - onClick: `onAnswerChange(question.id, value)`
    - Export as named export: `export function TfngRenderer`
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - components/listening/McqRenderer.tsx exists and contains `export function McqRenderer`
    - McqRenderer.tsx contains `aria-pressed=` on option buttons
    - McqRenderer.tsx contains `border-sky-400/50` for selected state
    - McqRenderer.tsx contains `text-[10px] font-semibold uppercase text-zinc-500 mr-2` for letter prefix
    - components/listening/TfngRenderer.tsx exists and contains `export function TfngRenderer`
    - TfngRenderer.tsx contains `role="group"` on the button container
    - TfngRenderer.tsx contains three values: `'true'`, `'false'`, `'not-given'`
    - TfngRenderer.tsx contains `border-sky-400/50` for selected state
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>McqRenderer renders radio-style option buttons with letter prefixes; TfngRenderer renders 3-button toggle row with True/False/Not Given labels</done>
</task>

<task type="auto">
  <name>Task 3: Build GapFillRenderer component</name>
  <files>components/listening/GapFillRenderer.tsx</files>
  <read_first>
    - lib/types.ts (GapFillQuestion interface -- after Task 1 adds sentence field)
    - .planning/phases/02-question-type-ui/02-UI-SPEC.md (GapFill renderer section)
    - .planning/phases/02-question-type-ui/02-RESEARCH.md (Pattern 4: GapFill Inline Input Sentence Split)
  </read_first>
  <action>
    Create `components/listening/GapFillRenderer.tsx` (per D-05, D-06):
    - `'use client'` directive at top
    - Import `GapFillQuestion` from `@/lib/types`
    - Props: `{ question: GapFillQuestion; answer: string; onAnswerChange: (id: string, value: string) => void; disabled?: boolean }`
    - Render question text label: `<p className="text-sm text-zinc-300"><span className="font-medium text-zinc-400">{question.id}.</span> {question.text}</p>`
    - Split `question.sentence` on `'___'`: `const parts = question.sentence.split('___')`
    - Render inline sentence with embedded input:
      ```
      <p className={`text-sm text-zinc-200 leading-relaxed mt-2 ${disabled ? 'opacity-60' : ''}`}>
        <span>{parts[0]}</span>
        <input
          className="inline-block w-32 border-b-2 border-zinc-600 bg-transparent text-sm text-zinc-100 text-center outline-none focus:border-sky-400 transition-colors duration-200 mx-1 px-1"
          aria-label="Fill in the blank"
          value={answer}
          onChange={(e) => onAnswerChange(question.id, e.target.value)}
          disabled={disabled}
        />
        <span>{parts[1]}</span>
      </p>
      ```
    - Export as named export: `export function GapFillRenderer`
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - components/listening/GapFillRenderer.tsx exists and contains `export function GapFillRenderer`
    - GapFillRenderer.tsx contains `question.sentence.split('___')`
    - GapFillRenderer.tsx contains `aria-label="Fill in the blank"`
    - GapFillRenderer.tsx contains `border-b-2 border-zinc-600` for the input underline style
    - GapFillRenderer.tsx contains `focus:border-sky-400` for focus state
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>GapFillRenderer splits sentence on ___ placeholder, renders inline input with bottom-border style, supports disabled state</done>
</task>

</tasks>

<verification>
All three renderer files compile without TypeScript errors. Each renderer exports a named function component. GapFillQuestion.sentence field exists in lib/types.ts. Generation prompt uses sentence field and tree-pine icon name.
</verification>

<success_criteria>
- `npx tsc --noEmit` passes with zero errors
- `npm run lint` passes
- Three new component files exist in components/listening/
- lib/types.ts GapFillQuestion includes sentence: string
- lib/prompts/listening.ts uses tree-pine and sentence field
</success_criteria>

<output>
After completion, create `.planning/phases/02-question-type-ui/02-01-SUMMARY.md`
</output>
