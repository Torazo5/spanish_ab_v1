---
phase: 03-grading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/grading.ts
  - app/api/listening/check-answers/route.ts
  - lib/prompts/listening.ts
  - app/listening/page.tsx
autonomous: true
requirements: [LISTEN-02, LISTEN-03]
must_haves:
  truths:
    - "MCQ, TFNG, icon-matching, person-attribution grade instantly without API call"
    - "Gap-fill questions are sent to AI and return explanatory feedback"
    - "Score is computed in marks from question.marks fields"
  artifacts:
    - path: "lib/grading.ts"
      provides: "Client-side deterministic grading function"
      exports: ["gradeLocally"]
    - path: "app/api/listening/check-answers/route.ts"
      provides: "AI grading for gap-fill only"
    - path: "app/listening/page.tsx"
      provides: "Split grading flow"
  key_links:
    - from: "app/listening/page.tsx"
      to: "lib/grading.ts"
      via: "import gradeLocally"
      pattern: "gradeLocally"
    - from: "app/listening/page.tsx"
      to: "/api/listening/check-answers"
      via: "fetch for gap-fill only"
      pattern: "gap-fill"
---

<objective>
Create client-side grading for deterministic types and update the API route to handle only gap-fill via AI.

Purpose: Instant feedback for deterministic questions; AI-powered feedback for gap-fill.
Output: grading utility, updated API route, updated page flow.
</objective>

<execution_context>
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/types.ts
@app/listening/page.tsx
@app/api/listening/check-answers/route.ts
@lib/prompts/listening.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grading utility and update API route</name>
  <files>lib/grading.ts, app/api/listening/check-answers/route.ts, lib/prompts/listening.ts</files>
  <read_first>lib/types.ts, app/api/listening/check-answers/route.ts, lib/prompts/listening.ts</read_first>
  <action>
    Create lib/grading.ts exporting gradeLocally(questions, answers) that returns AnswerResult[] with marks. For each deterministic type, compare answers[q.id] to the correct field (correctIndex for MCQ, correctAnswer for TFNG, correctIconName for icon-matching, correctAnswer for person-attribution). Set feedback to a short explanation showing the correct answer.

    Update route.ts to accept {script, questions: TypedListeningQuestion[], answers: Record<string, string>}. Filter to gap-fill questions only. Update checkAnswersPrompt to accept Record<string, string> and map by q.id instead of index. Return results with marks per question.
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/grading.ts exports gradeLocally
    - route.ts imports TypedListeningQuestion (not ListeningQuestion)
    - route.ts destructures answers as Record<string, string>
    - checkAnswersPrompt uses answers[q.id] pattern
  </acceptance_criteria>
  <done>Deterministic grading logic exists; API route accepts only gap-fill; types pass tsc</done>
</task>

<task type="auto">
  <name>Task 2: Wire split grading into page</name>
  <files>app/listening/page.tsx</files>
  <read_first>app/listening/page.tsx, lib/grading.ts</read_first>
  <action>
    Update checkAnswers in page.tsx: import gradeLocally. Call gradeLocally for all deterministic questions. If gap-fill questions exist, send only those to the API. Merge both result arrays. Compute totalScore by summing marks from matched questions where correct=true. Set maxScore from script.totalMarks. Set results state with merged array.
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports gradeLocally from lib/grading
    - page.tsx filters questions by type for API call
    - page.tsx computes totalScore from marks fields
  </acceptance_criteria>
  <done>Submit grades deterministic types locally, sends only gap-fill to API, merges results</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — no type errors. Run `npm run build` — builds successfully.
</verification>

<success_criteria>
Deterministic questions grade without API call. Gap-fill questions go to AI. Score computed in marks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-grading/03-01-SUMMARY.md`
</output>
