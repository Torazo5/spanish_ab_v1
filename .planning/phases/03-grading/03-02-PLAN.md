---
phase: 03-grading
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/listening/FeedbackPanel.tsx
  - lib/types.ts
autonomous: false
requirements: [LISTEN-02, LISTEN-03]
must_haves:
  truths:
    - "User sees total score in marks after submission"
    - "Correct answers are visually distinguished from incorrect"
    - "Gap-fill results show AI explanatory feedback"
    - "Deterministic results show the correct answer"
  artifacts:
    - path: "components/listening/FeedbackPanel.tsx"
      provides: "Updated feedback display with marks and correct answers"
    - path: "lib/types.ts"
      provides: "Extended AnswerResult with marks field"
  key_links:
    - from: "components/listening/FeedbackPanel.tsx"
      to: "lib/types.ts"
      via: "AnswerResult with marks"
      pattern: "AnswerResult"
---

<objective>
Update FeedbackPanel to display marks-based scoring and show correct answers for wrong responses.

Purpose: Users see meaningful grading feedback with marks and correct answers.
Output: Enhanced FeedbackPanel, updated AnswerResult type.
</objective>

<execution_context>
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/types.ts
@components/listening/FeedbackPanel.tsx
@.planning/phases/03-grading/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AnswerResult and update FeedbackPanel</name>
  <files>lib/types.ts, components/listening/FeedbackPanel.tsx</files>
  <read_first>lib/types.ts, components/listening/FeedbackPanel.tsx</read_first>
  <action>
    Add marks and correctAnswer fields to AnswerResult in types.ts. Update FeedbackPanel to display each result with its marks value, show the correct answer when the student was wrong, and display the AI feedback string for gap-fill results. Compute totalScore by summing marks of correct results. Show score as "X / Y marks".
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - AnswerResult has marks field of type number
    - AnswerResult has correctAnswer field of type string
    - FeedbackPanel renders correctAnswer when result.correct is false
    - FeedbackPanel shows marks in score display
  </acceptance_criteria>
  <done>FeedbackPanel shows marks-based score, correct answers for wrong responses, AI feedback for gap-fill</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify grading flow end-to-end</name>
  <files>app/listening/page.tsx</files>
  <action>
    No code changes. Human verifies the complete grading flow works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npm run build</automated>
  </verify>
  <what-built>Complete grading system: client-side grading for deterministic types, AI grading for gap-fill, marks-based scoring, correct/incorrect visual distinction</what-built>
  <how-to-verify>
    1. Run npm run dev, open localhost:3000/listening
    2. Generate an exercise (10 marks)
    3. Answer some questions correctly and some incorrectly
    4. Click Check Answers
    5. Verify: deterministic types grade instantly (no loading delay for those)
    6. Verify: gap-fill shows AI feedback text
    7. Verify: total score shown in marks (e.g. "7 / 10 marks")
    8. Verify: correct answers show green, incorrect show red with correct answer
  </how-to-verify>
  <acceptance_criteria>
    - Build passes with npm run build
    - Human confirms grading results display correctly
  </acceptance_criteria>
  <done>Human confirms grading flow works end-to-end</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
`npm run build` passes. Visual check confirms marks display and correct/incorrect distinction.
</verification>

<success_criteria>
Score shown in marks. Correct/incorrect visually distinct. Gap-fill shows AI feedback. Deterministic shows correct answer on wrong response.
</success_criteria>

<output>
After completion, create `.planning/phases/03-grading/03-02-SUMMARY.md`
</output>
