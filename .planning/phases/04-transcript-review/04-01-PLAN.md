---
phase: 04-transcript-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/listening/TranscriptPanel.tsx
  - app/listening/page.tsx
autonomous: true
requirements:
  - LISTEN-06
must_haves:
  truths:
    - "User can see a 'Reveal transcript' toggle after generating a session"
    - "Clicking the toggle shows the full Spanish script text"
    - "Clicking again hides the transcript"
    - "The toggle is available in loaded, answered, and review states but NOT in setup"
  artifacts:
    - path: "components/listening/TranscriptPanel.tsx"
      provides: "Collapsible transcript panel component"
      exports: ["TranscriptPanel"]
      min_lines: 20
    - path: "app/listening/page.tsx"
      provides: "Page with transcript panel integrated and extended PageState"
      contains: "'review'"
  key_links:
    - from: "app/listening/page.tsx"
      to: "components/listening/TranscriptPanel.tsx"
      via: "import and render with script.script prop"
      pattern: "<TranscriptPanel"
---

<objective>
Create a collapsible TranscriptPanel component and integrate it into the listening page so users can reveal the full Spanish script at any point after generation.

Purpose: Satisfies LISTEN-06 — transcript visibility at any session stage. Also extends PageState with 'review' to prepare for Plan 02.
Output: TranscriptPanel.tsx component, updated page.tsx with transcript integration and extended PageState type.
</objective>

<execution_context>
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transcript-review/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/listening/page.tsx:
```typescript
type PageState = 'setup' | 'loaded' | 'answered'
// script state holds TypedListeningScript which has a .script string field
const [script, setScript] = useState<TypedListeningScript | null>(null)
```

From lib/types.ts:
```typescript
export interface TypedListeningScript {
  title: string
  script: string
  totalMarks: number
  questions: TypedListeningQuestion[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TranscriptPanel component</name>
  <files>components/listening/TranscriptPanel.tsx</files>
  <read_first>
    - components/listening/ListeningPlayer.tsx (existing component pattern for styling consistency)
    - components/listening/FeedbackPanel.tsx (existing component pattern)
    - app/listening/page.tsx (understand how components are rendered in Card wrappers)
  </read_first>
  <action>
Create `components/listening/TranscriptPanel.tsx` as a client component ('use client').

Props interface:
```typescript
interface TranscriptPanelProps {
  script: string
  defaultOpen?: boolean
}
```

Implementation:
- Import `useState` from React, `ChevronDown` and `BookOpen` from lucide-react
- Internal `open` state initialized from `defaultOpen` prop (default false)
- Render a container div with classes: `rounded-xl border border-white/10 bg-zinc-900/60 overflow-hidden`
- Render a button that toggles `open`:
  - Classes: `flex w-full items-center justify-between px-4 py-3 text-sm font-medium text-zinc-300 hover:text-white transition-colors`
  - Left side: BookOpen icon (w-4 h-4) + text that reads `'Hide transcript'` when open, `'Reveal transcript'` when closed
  - Right side: ChevronDown icon (w-4 h-4) with `transition-transform` and `rotate-180` when open
- When `open` is true, render the script text in a div:
  - Classes: `px-4 pb-4 text-sm text-zinc-300 leading-relaxed whitespace-pre-wrap border-t border-white/10 pt-3`
  - Content: `{script}` (the script string prop)

Export as named export: `export function TranscriptPanel({ script, defaultOpen = false }: TranscriptPanelProps)`
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/listening/TranscriptPanel.tsx exists
    - File contains `'use client'` on line 1
    - File contains `export function TranscriptPanel`
    - File contains `interface TranscriptPanelProps` with `script: string` and `defaultOpen?: boolean`
    - File contains `useState` for open/closed state
    - File contains `BookOpen` and `ChevronDown` imports from lucide-react
    - File contains text `'Reveal transcript'` and `'Hide transcript'`
    - File contains `whitespace-pre-wrap` class for script text rendering
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>TranscriptPanel.tsx is a working client component that toggles transcript visibility with BookOpen icon and chevron indicator</done>
</task>

<task type="auto">
  <name>Task 2: Integrate TranscriptPanel into listening page and extend PageState</name>
  <files>app/listening/page.tsx</files>
  <read_first>
    - app/listening/page.tsx (current full file — must see exact render structure and line numbers)
    - components/listening/TranscriptPanel.tsx (just created — verify export name)
  </read_first>
  <action>
Modify `app/listening/page.tsx` with these specific changes:

1. **Add import** at top with other listening component imports:
   ```typescript
   import { TranscriptPanel } from '@/components/listening/TranscriptPanel'
   ```

2. **Extend PageState type** (line 14):
   Change `type PageState = 'setup' | 'loaded' | 'answered'`
   To: `type PageState = 'setup' | 'loaded' | 'answered' | 'review'`

3. **Add TranscriptPanel rendering** — insert a new Card block between the ListeningPlayer card (the `{script && (` block around line 231) and the Questions card (the `{script && pageState !== 'answered' && (` block around line 237).

   Insert this JSX:
   ```tsx
   {/* Transcript — visible after generation, all non-setup states */}
   {script && pageState !== 'setup' && (
     <Card className="border-white/10 bg-gradient-to-br from-zinc-900 via-zinc-900 to-black p-5 shadow-[0_20px_60px_-40px_rgba(0,0,0,0.9)]">
       <TranscriptPanel script={script.script} defaultOpen={pageState === 'review'} />
     </Card>
   )}
   ```

4. **Update the Questions section condition** (currently `pageState !== 'answered'` around line 237):
   Change `{script && pageState !== 'answered' && (` 
   To: `{script && (pageState === 'loaded') && (`
   
   This ensures questions are only shown in loaded state (not in answered or review — review mode will be added in Plan 02).

5. **Update the Results section condition** (currently `{results && (` around line 260):
   Change `{results && (`
   To: `{results && pageState === 'answered' && (`
   
   This ensures the results/feedback card only shows in answered state, not in review state (review rendering added in Plan 02).

Do NOT add reviewAnswers state yet — that belongs to Plan 02.
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/listening/page.tsx contains `import { TranscriptPanel } from '@/components/listening/TranscriptPanel'`
    - app/listening/page.tsx contains `type PageState = 'setup' | 'loaded' | 'answered' | 'review'`
    - app/listening/page.tsx contains `<TranscriptPanel script={script.script} defaultOpen={pageState === 'review'} />`
    - app/listening/page.tsx contains `pageState !== 'setup'` condition for TranscriptPanel rendering
    - app/listening/page.tsx contains `pageState === 'loaded'` condition for questions section (not `!== 'answered'`)
    - app/listening/page.tsx contains `pageState === 'answered'` condition for results section
    - npm run build exits 0
  </acceptance_criteria>
  <done>TranscriptPanel is visible in loaded, answered, and review states; PageState includes 'review'; questions only show in loaded state; results only show in answered state; build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no TypeScript errors
- `npm run lint` passes
- TranscriptPanel.tsx exists with correct props interface and toggle behavior
- page.tsx imports and renders TranscriptPanel gated on `script && pageState !== 'setup'`
- PageState type includes all four values: setup, loaded, answered, review
</verification>

<success_criteria>
- TranscriptPanel component created with collapsible toggle, BookOpen icon, and defaultOpen prop
- Transcript visible in all post-generation states (loaded, answered, review)
- Transcript NOT visible in setup state
- Questions section gated to loaded state only
- Results section gated to answered state only
- Full build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-transcript-review/04-01-SUMMARY.md`
</output>
