---
phase: 01-generation-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/listening/page.tsx
autonomous: false
requirements:
  - LISTEN-09
must_haves:
  truths:
    - "Page loads with 10-mark chip selected by default"
    - "User can click a different mark chip and it becomes selected"
    - "Clicking Generate Exercise sends the selected marks value to the API"
    - "All chips are disabled while loading"
  artifacts:
    - path: "app/listening/page.tsx"
      provides: "MarkSelector UI and marks state wired to API call"
      contains: "MARK_OPTIONS"
  key_links:
    - from: "app/listening/page.tsx"
      to: "/api/listening/generate-script"
      via: "fetch POST body includes marks"
      pattern: "marks"
    - from: "app/listening/page.tsx"
      to: "lib/types.ts"
      via: "imports MARK_OPTIONS and MarkOption"
      pattern: "import.*MARK_OPTIONS"
---

<objective>
Add the MarkSelector UI to the listening setup card, allowing users to choose total marks (5, 10, 15, 25) before generating an exercise.

Purpose: Complete the user-facing part of LISTEN-09 — mark selection controls the generation output.
Output: Updated listening page with mark selector wired to the API.
</objective>

<execution_context>
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/torazotokuda/code_testing/spanish_ab_v1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-generation-foundation/01-UI-SPEC.md
@.planning/phases/01-generation-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan imports -->

From lib/types.ts:
```typescript
export const MARK_OPTIONS = [5, 10, 15, 25] as const
export type MarkOption = (typeof MARK_OPTIONS)[number]
```

From app/listening/page.tsx (current state variable pattern):
```typescript
const [topic, setTopic] = useState<IbTopic>('school')
// API call in generateScript():
body: JSON.stringify({ topic })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add MarkSelector section and wire marks to API call</name>
  <files>app/listening/page.tsx</files>
  <read_first>
    - app/listening/page.tsx (current full page — must see exact structure to insert into)
    - lib/types.ts (to verify MARK_OPTIONS and MarkOption exports exist from Plan 01)
    - .planning/phases/01-generation-foundation/01-UI-SPEC.md (exact classes, copy, layout for MarkSelector)
  </read_first>
  <action>
Modify app/listening/page.tsx to add mark selection. Follow the UI-SPEC exactly for all visual details.

1. Add imports at top:
```typescript
import { MARK_OPTIONS, type MarkOption } from '@/lib/types'
```

2. Add state variable after the existing `topic` state (line ~16):
```typescript
const [marks, setMarks] = useState<MarkOption>(10)
```

3. Insert the Mark Target section inside the setup Card, AFTER the topic grid div (after the closing `</div>` of `grid grid-cols-2 gap-2`) and BEFORE the error display. Add this JSX block:

```jsx
{/* Mark Target section */}
<div className="space-y-1">
  <p className="text-[11px] font-semibold uppercase tracking-[0.22em] text-sky-300/70">Mark Target</p>
  <h2 className="text-lg font-semibold text-white">How many marks?</h2>
  <p className="text-sm text-zinc-400">Total marks determines how many questions are generated.</p>
</div>

<div className="grid grid-cols-4 gap-2">
  {MARK_OPTIONS.map((m) => (
    <button
      key={m}
      onClick={() => setMarks(m)}
      disabled={loading}
      className={`group relative overflow-hidden rounded-2xl border px-4 py-3 text-center transition-all duration-200 active:scale-[0.99] ${
        loading ? 'pointer-events-none opacity-60' : ''
      } ${
        marks === m
          ? 'border-sky-400/50 bg-gradient-to-br from-sky-400/20 via-sky-500/10 to-zinc-950 text-white shadow-[0_20px_45px_-30px_rgba(56,189,248,0.95)]'
          : 'border-white/10 bg-gradient-to-br from-zinc-800 via-zinc-900 to-zinc-950 text-zinc-200 hover:border-sky-400/25 hover:text-white'
      }`}
    >
      <span className="relative flex flex-col items-center gap-0.5">
        <span className="text-sm font-semibold">{m}</span>
        <span className="text-[10px] font-semibold uppercase tracking-wide text-zinc-500">marks</span>
      </span>
      <span
        className={`absolute right-2 top-2 h-2.5 w-2.5 rounded-full transition-all ${
          marks === m ? 'bg-sky-300 shadow-[0_0_0_6px_rgba(125,211,252,0.13)]' : 'bg-zinc-600'
        }`}
      />
    </button>
  ))}
</div>
```

4. Update the fetch call in `generateScript` to include marks in the POST body. Change:
```typescript
body: JSON.stringify({ topic }),
```
to:
```typescript
body: JSON.stringify({ topic, marks }),
```

That is the ONLY change to the generateScript function.

Do NOT modify any other sections of the page (ListeningPlayer, QuestionPanel, FeedbackPanel, checkAnswers, header, etc.).
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npx tsc --noEmit 2>&1 | head -30 && echo "---BUILD---" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/listening/page.tsx contains `import { MARK_OPTIONS, type MarkOption } from '@/lib/types'`
    - app/listening/page.tsx contains `useState<MarkOption>(10)`
    - app/listening/page.tsx contains `Mark Target`
    - app/listening/page.tsx contains `How many marks?`
    - app/listening/page.tsx contains `Total marks determines how many questions are generated.`
    - app/listening/page.tsx contains `MARK_OPTIONS.map`
    - app/listening/page.tsx contains `grid grid-cols-4 gap-2`
    - app/listening/page.tsx contains `setMarks(m)`
    - app/listening/page.tsx contains `pointer-events-none opacity-60`
    - app/listening/page.tsx contains `JSON.stringify({ topic, marks })`
    - app/listening/page.tsx contains `bg-sky-300 shadow-[0_0_0_6px_rgba(125,211,252,0.13)]` (selected dot)
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>MarkSelector renders 4 chips (5/10/15/25), 10 selected by default, selected marks sent in API POST body, chips disabled during loading, all styling per UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify mark selector UI and end-to-end generation</name>
  <files>app/listening/page.tsx</files>
  <action>
Human verifies the mark selector UI and end-to-end generation flow visually in the browser. No code changes — this is a visual/functional checkpoint.
  </action>
  <verify>
    <automated>cd /Users/torazotokuda/code_testing/spanish_ab_v1 && export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms: 4 mark chips visible (5, 10, 15, 25) in setup card
    - Human confirms: 10-mark chip selected by default with sky-blue accent
    - Human confirms: clicking a different chip selects it and deselects others
    - Human confirms: Generate Exercise sends marks value in request body
  </acceptance_criteria>
  <done>User has visually verified the mark selector UI matches the UI-SPEC and the end-to-end flow works.</done>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/listening
    2. Verify the setup card shows:
       - Topic grid (unchanged)
       - "Mark Target" eyebrow, "How many marks?" heading, description text
       - 4 chips: 5, 10, 15, 25 — each showing the number and "marks" label
       - 10 is selected by default (sky blue border/glow, sky dot in top-right)
    3. Click "5 marks" chip — it becomes selected, "10" deselects
    4. Click "Generate Exercise" with 5 marks selected
    5. While loading: all mark chips should be disabled (dimmed, not clickable)
    6. After generation succeeds: open browser DevTools Network tab, find the POST to /api/listening/generate-script, verify the request body contains `"marks": 5`
    7. (Optional) Try with 25 marks — expect more questions in the response
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes — no type or build errors
2. Mark selector renders with correct styling per UI-SPEC
3. Default selection is 10 marks
4. API call includes marks in request body
5. Generation returns JSON with mixed question types
</verification>

<success_criteria>
- MarkSelector visible in setup card between topic grid and Generate button
- 10-mark chip selected by default
- Selected chip has sky-400 accent styling, unselected chips have zinc styling
- Chips disabled during loading
- API POST body includes marks value
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-generation-foundation/01-02-SUMMARY.md`
</output>
